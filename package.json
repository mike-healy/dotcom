{
  "name": "mike_11ty",
  "version": "1.0.0",
  "description": "",
  "main": "index.js",
  "scripts": {
    "tailwind": "npx tailwindcss -i ./src/style_input.css -o ./src/style.css --watch",
    "test": "echo \"Error: no test specified\" && exit 1",
    "build": "tailwindcss -i ./src/style_input.css -o ./src/style.css --minify && npx @11ty/eleventy && exit 1"
  },
  "keywords": [],
  "author": "",
  "license": "ISC",
  "devDependencies": {
    "@11ty/eleventy": "^2.0.1",
    "tailwindcss": "^3.4.1"
  }
}
